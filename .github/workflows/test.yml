name: nightly

on:
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@eb908c0048dc747119795dcd48480e1cbf3575ee # tag=cargo-llvm-cov
